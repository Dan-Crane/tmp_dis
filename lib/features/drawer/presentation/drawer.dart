import 'package:flutter/material.dart';

class DiscordDrawer extends StatelessWidget {
  const DiscordDrawer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Drawer();
  }
}